
struct VS_OUT {
   float4 Pos : SV_POSITION;
   float4 Tex : TEXCOORD;
};


Texture2D g_tex : register(t0);
SamplerState g_sampler : register(s0);

float4 RTT_PS(VS_OUT vs_out) : SV_Target
{
  
  float4 texColor = g_tex.Sample(g_sampler, vs_out.Tex);

  return texColor;
}